version: '3.8'

services:
  consistent-hash-lab:
    build: .
    container_name: consistent-hash-lab
    volumes:
      - .:/app
      - /app/__pycache__
    working_dir: /app
    stdin_open: true
    tty: true
    command: bash
